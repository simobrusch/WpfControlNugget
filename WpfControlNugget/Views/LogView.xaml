<UserControl x:Class="WpfControlNugget.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfControlNugget.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Height="auto" Width="625" Background="#FF89A9B2" >
        <TextBlock Height="32" HorizontalAlignment="Left" Margin="16,15,0,0" Name="TextBlockHeading"
Text="LogReader" VerticalAlignment="Top" Width="310"  FontSize="20" FontStretch="Normal"/>
        <Grid HorizontalAlignment="Left" Height="310" VerticalAlignment="Top" Width="625">
            <DataGrid AutoGenerateColumns="False" HorizontalAlignment="Left" SelectedItem="{Binding MySelectedItem, Mode=TwoWay}" Margin="14,55,0,46" Name="DataGridCustomers" Width="575" ItemsSource="{Binding Path=Logs}"
CanUserResizeRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=id}" Header="id" Width="80" IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Path=pod}" Header="pod" Width="80" IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Path=location}" Header="location" Width="80" IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Path=hostname}" Header="hostname" Width="80" IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Path=severity}" Header="severity" Width="80" IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Path=timestamp}" Header="timestamp" Width="80" IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Path=message}" Header="message" Width="120" IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
            <Label Content="Database Connection" HorizontalAlignment="Left" Margin="150,18,0,0" VerticalAlignment="Top" Height="23" Width="124" Background="{x:Null}"/>
            <TextBox HorizontalAlignment="Left" Height="23" Margin="279,22,0,0" TextWrapping="Wrap" Text="{Binding TxtConnectionString}" Name="TxtConnectionString"  VerticalAlignment="Top" Width="310" />
            <Button Content="Load Data" Height="25" HorizontalAlignment="Left" Margin="279,275,0,0" Name="Btnloaddata" VerticalAlignment="Top" Width="100" Command="{Binding BtnLoadDataClick}"/>
            <Button Content="Confirm" Height="25" HorizontalAlignment="Left" Margin="489,275,0,0" Name="BtnConfirm" VerticalAlignment="Top" Width="100" Command="{Binding BtnConfirmDataClick}"/>
            <Button Content="Add" Height="25" HorizontalAlignment="Left" Margin="384,275,0,0" Name="BtnAdd" VerticalAlignment="Top" Width="100" Command="{Binding BtnAddDataClick}"/>
        </Grid>
    </Grid>
</UserControl>
